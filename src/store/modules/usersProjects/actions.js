import api from '../../../api/users-projects-api';
import * as types from './mutation-types';

// TODO
const fetchProjectsByUserIds = (context, userIds) => {
  // api.getProjectsByUserIds(userIds)
};

export default {
  fetchProjectsByUserIds,
};
